build_exec(
    EXECNAME warehouse-isac
    SOURCE_FILES
        warehouse-isac-main.cc
        warehouse-scenario.cc
    LIBRARIES_TO_LINK
        ${libantenna}
        ${libapplications}
        ${libcore}
        ${libenergy}
        ${libflow-monitor}
        ${libinternet}
        ${libmobility}
        ${libnr}
        ${libpoint-to-point}
        ${libpropagation}
        ${libsionnart}
        ${libspectrum}
        ${libmqtt}
        ${libwarehouse}
    EXECUTABLE_DIRECTORY_PATH
        ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/scratch/warehouse-isac/
)

build_exec(
    EXECNAME warehouse-no-isac
    SOURCE_FILES
        warehouse-no-isac-main.cc
        warehouse-scenario.cc
    LIBRARIES_TO_LINK
        ${libantenna}
        ${libapplications}
        ${libcore}
        ${libenergy}
        ${libflow-monitor}
        ${libinternet}
        ${libmobility}
        ${libnr}
        ${libpoint-to-point}
        ${libpropagation}
        ${libsionnart}
        ${libspectrum}
        ${libmqtt}
        ${libwarehouse}
    EXECUTABLE_DIRECTORY_PATH
        ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/scratch/warehouse-isac/
)
